name: Extrakt a Theme

on:
  issues:
    types: [ "opened", "tagged", "edited" ]

  workflow_dispatch:
  
jobs:
  extrakt:
    runs-on: ubuntu-latest

      - name: Do Stuff
        run: |
          echo ${{ github.event }}
          echo ${{ github.event.issue }}
