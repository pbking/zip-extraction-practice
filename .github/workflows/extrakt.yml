name: Extrakt a Theme

on:
  issues:
    types: [ "opened", "tagged", "edited" ]

    workflow_dispatch:
  
jobs:
  extrakt:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          
      - name: Get Zip file
        run: |
          wget https://github.com/pbking/zip-extraction-practice/files/10561056/adventurer.zip
          
      - uses: montudor/action-zip@v1
        with:
          args: unzip -qq adventurer.zip  
          
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4
        with:
          commit-message: Add adventurer to repo
          committer: GitHub <noreply@github.com>
          author: ${{ github.actor }} <${{ github.actor }}@users.noreply.github.com>
          signoff: false
          branch: add/adventurer
          delete-branch: true
          title: '[Add Theme] Adventurer'
          body: |
            Add Adventurer THeme
            - Excracted from zip
          labels: |
            add theme
          assignees: pbking
          reviewers: pbking
          team-reviewers: |
            owners
            maintainers
          draft: false
